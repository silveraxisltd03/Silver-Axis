[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit"

# SPA fallback so client-side routes (/services, /portfolio, /contact) work on reload
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
